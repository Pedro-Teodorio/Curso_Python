"""
 TODO: Tipo None
    * O tipo de dado None em Python repesenta o tipo sem tipo, ou pode ser  conhecido também como tipo vazio,
    * porém, falar que é um tipo sem tipo é mais apropriado.
    ? OBS: O tipo None é SEMPRE espesificado com a primeira letra maiúscula.

    * CERTO: None
    ! Errado: none

    * Quando utilizamos ?
        * - Podemos utilizar None quando queremos criar uma variavel e inicializá-la com um tipo sem tipo, 
        * antes de recebermos um valor final. 
"""

numeros = None
print(numeros)
print(type(numeros))
#----------------------------
numeros  = 1.44, 1.34, 5.67
print(numeros)
print(type(numeros))