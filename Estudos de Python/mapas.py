"""
TODO: Mapas -> Conhecidos em Python com Dicionários
* Dicionários em Python são representados por  chaves {}*

    TODO: Iterar sobre Dicionários:

        * receita = {'Janeiro': 100,'Fevereiro': 300, 'Março': 150}
        * for chave in receita:
            * print(f"Em {chave} recebi R$ {receita[chave]}")

    TODO: Acessando as Chaves:

        * receita = {'Janeiro': 100,'Fevereiro': 300, 'Março': 150}
        * for chave in receita.keys():
            * print(receita[chave])

    TODO: Acessando os valores:

        * receita = {'Janeiro': 100,'Fevereiro': 300, 'Março': 150}
        * for valor in receita.values():
            * print(valor)

    TODO: Desempacotamento de Dicionários:

        * receita = {'Janeiro': 100,'Fevereiro': 300, 'Março': 150}
        * for chave, valor in receita.items():
            * print(f'chave = {chave} e valor = {valor}')

    TODO: Soma*, Valor Máximo*, Valor Minimo*, Tamanho:
    
        * receita = {'Janeiro': 100,'Fevereiro': 300, 'Março': 150}
        ? OBS: (*) Se os valores forem todos inteiros ou reais.
        ! Soma
            * print(sum(receita.values()))
        ! Valor Máximo
            * print(max(receita.values()))
        ! Valor Minimo
            * print(min(receita.values()))
        ! Tamanho
            * print(len(receita))
"""








